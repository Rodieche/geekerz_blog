<p id="notice"><%= notice %></p>

<h3 class="center jackpot upfont margin-top hide-on-small-only", style="color:<%= @category.color %>;"><%= @category.name %></h3>

<h3 class="center jackpot upfont margin-top hide-on-med-and-up title-mobile", style="color:<%= @category.color %>;"><%= @category.name %></h3>
<div class="row">
	<%= render @category.articles %>
</div>
